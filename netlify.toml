# Netlify configuration for TanStack Start
# The @netlify/vite-plugin-tanstack-start plugin handles most of the setup

[build]
  command = "npm run build"
  # Don't set publish - let Netlify auto-detect from the plugin

